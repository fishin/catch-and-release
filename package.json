{
  "name": "catch-and-release",
  "version": "0.0.2",
  "description": "build and deploy functions for ficion",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/fishin/catch-and-release"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fishin/catch-and-release/issues"
  },
  "homepage": "https://github.com/fishin/catch-and-release",
  "engines": {
    "node": ">=0.12.x"
  },
  "scripts": {
    "test": "lab -a code -t 100 -v -m 30000 -L -r json -o lab.json -r html -o lab.html -r console -o stdout -r console -o console.log",
    "console": "lab -a code -t 100 -v -m 30000 -L"
  },
  "dependencies": {
    "hoek": "2.x.x",
    "smelt": "git+https://github.com/fishin/smelt"
  },
  "devDependencies": {
    "code": "1.x.x",
    "hapi": "8.x.x",
    "lab": "5.x.x",
    "mock": "git+https://github.com/fishin/mock"
  }
}
